import { OwnerOfShoppinglistPipe } from './owner-of-shoppinglist.pipe';

describe('OwnerOfShoppinglistPipe', () => {
  it('create an instance', () => {
    const pipe = new OwnerOfShoppinglistPipe();
    expect(pipe).toBeTruthy();
  });
});
