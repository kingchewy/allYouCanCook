import { UidToFriendsNamePipe } from './uid-to-friends-name.pipe';

describe('UidToFriendsNamePipe', () => {
  it('create an instance', () => {
    const pipe = new UidToFriendsNamePipe();
    expect(pipe).toBeTruthy();
  });
});
