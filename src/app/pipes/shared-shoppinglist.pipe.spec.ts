import { SharedShoppinglistPipe } from './shared-shoppinglist.pipe';

describe('SharedShoppinglistPipe', () => {
  it('create an instance', () => {
    const pipe = new SharedShoppinglistPipe();
    expect(pipe).toBeTruthy();
  });
});
